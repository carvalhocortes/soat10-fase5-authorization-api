# Configurações da AWS
aws_region     = "us-east-1"
aws_account_id = "123456789012"  # Substitua pelo seu Account ID

# Configuração do JWT Secret
ssm_jwt_secret_name  = "/auth/jwt-secret"
ssm_jwt_secret_value = "your-super-secret-jwt-key-here"  # Gere uma chave segura

# Exemplo de como gerar uma chave segura:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
